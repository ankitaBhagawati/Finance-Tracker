@{
    ViewData["Title"] = "Welcome";
}

@* <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <title>Dashboard - Finance Tracker</title> *@
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <style>
        body {
            background-color: #eaf1fb;
        }

        .header {
            text-align: center;
            margin-bottom: 40px;
        }

            .header h2 {
                color: #0d6efd;
                font-weight: 600;
            }

        .card {
            border: none;
            border-radius: 12px;
            transition: transform 0.2s ease-in-out;
        }

            .card:hover {
                transform: translatey(-5px);
                box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
            }

        .btn-custom {
            background-color: #9ebeef;
            color: black;
            border-radius: 8px;
        }

            .btn-custom:hover {
                background-color: #eaf1fb;
            }
    </style>
@* </head>
<body> *@
    <div class="container py-5">
        <div class="header">
            <h1 class="mb-2">Track. Budget. Thrive.</h1>
        </div>

        <div class="row g-4 justify-content-center">
            <div class="col-md-4">
                <div class="card p-4 text-center bg-white">
                    <h5 class="mb-3">Track every rupee, master your day</h5>
                <a href="/Expense/Index" class="btn btn-custom w-100">Expense</a>
                </div>
            </div>
            <div class="col-md-4">
                <div class="card p-4 text-center bg-white">
                    <h5 class="mb-3">Plan smart, spend smarter</h5>
                    <a href="/Budget/Index" class="btn btn-custom w-100">Monthly Budget</a>
                </div>
            </div>
@*             <div class="col-md-4">
                <div class="card p-4 text-center bg-white">
                    <h5 class="mb-3">Log your income, own your future.</h5>
                    <a href="#" class="btn btn-custom w-100">Salary</a>
                </div>
            </div> *@
        </div>
    </div>
@* </body>
</html> *@
